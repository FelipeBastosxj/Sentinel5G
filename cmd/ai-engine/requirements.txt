# Pinned baseline for `pip install -r requirements.txt` (see CLAUDE.md).
# The canonical dependency set lives in pyproject.toml; keep this in sync
# with its [project.dependencies] whenever that list changes. This is the
# serving-path-only set -- to train/export/run the full test suite, also
# install the "train" extra (`pip install -e ".[dev,train]"`), which adds
# torch/onnx/onnxscript.
onnxruntime>=1.18,<2.0
numpy>=1.26,<3.0
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
nats-py>=2.7,<3.0
