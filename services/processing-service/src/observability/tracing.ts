/**
 * Tracing stub — OTel removed from MVP.
 * Can be re-introduced via a Docker Compose observability profile in Phase 5.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function startTracing(_serviceName: string): void {
  // no-op
}
